name: VeloTV EPG Generator
on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  merge-epg:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install requests pyyaml

      - name: Run EPG Merge
        run: |
          python - <<EOF
          import requests, gzip, yaml, os
          
          # 1. Load your list
          try:
              with open('xmlmerge.yaml', 'r') as f:
                  config = yaml.safe_load(f)
              urls = config.get('files', [])
              print(f"Syncing {len(urls)} sources for VeloTV...")
          except Exception as e:
              print(f"YAML Error: {e}")
              exit(1)

          # 2. Process and Merge
          headers = {'User-Agent': 'Mozilla/5.0 (VeloTV-Bot/1.0)'}
          with open('epg.xml', 'wb') as out:
              out.write(b'<?xml version="1.0" encoding="UTF-8"?><tv>')
              for url in urls:
                  print(f"Fetching: {url}")
                  try:
                      r = requests.get(url, headers=headers, timeout=60)
                      if r.status_code == 200:
                          content = r.content
                          if url.endswith('.gz'):
                              content = gzip.decompress(content)
                          
                          # Remove tags to allow clean concatenation
                          clean = content.replace(b'<?xml version="1.0" encoding="UTF-8"?>', b'')
                          clean = clean.replace(b'<tv>', b'').replace(b'</tv>', b'')
                          out.write(clean)
                          print(f"  Success: {len(content)} bytes")
                      else:
                          print(f"  HTTP {r.status_code} for {url}")
                  except Exception as e:
                      print(f"  Failed {url}: {e}")
              out.write(b'</tv>')
          EOF

      - name: Deploy Result
        run: |
          gzip -9 -c epg.xml > epg.xml.gz
          git config --global user.name "VeloTV-Bot"
          git config --global user.email "bot@velotv.app"
          git add epg.xml.gz
          git commit -m "Update EPG Data" || echo "No changes"
          git push
